{% extends 'base.html.twig' %}

{% block title %}Supprimer un Espace{% endblock %}

{% block body %}
    <h1>Supprimer l'Espace' <i>{{ espace.nom }} ?</i></h1>
    {{ form(formulaire) }}
    <a href="{{ path("app_espace_voir") }}" class="btn btn-default">Retour</a>
{% endblock %}
